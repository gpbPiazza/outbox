version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/wes/sql/queries.sql"
    schema: "infra/databases/sql/migrations"
    gen:
      go:
        package: "wesdb"
        out: "internal/wes/db"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
        rename:
          we: "Wes"
        overrides:
          - db_type: "uuid"
            go_type:
              import: "github.com/google/uuid"
              type: "UUID"
